{{define "dashboard"}}
{{template "admin_header" .}}
{{template "navbar" .}}
<div class="container">
	<div class="mt-5">
		<h1 class="text-center">The dashboard</h1>
	</div>
</div>
{{template "footer" .}}
{{end}}
