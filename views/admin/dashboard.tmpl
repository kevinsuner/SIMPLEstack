{{define "dashboard"}}
{{template "admin_header" .}}
{{template "navbar" .}}
<div class="container">
	<!-- TOOD: this should only be displayed when TOKEN cookie exists -->
	<div class="mt-5">
		<h1 class="text-center">The dashboard</h1>
	</div>
</div>
{{template "footer" .}}
{{end}}
