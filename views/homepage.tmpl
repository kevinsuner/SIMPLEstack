{{define "homepage"}}
{{template "header" .}}
{{template "navbar" .}}
<div class="container">
	<div class="mt-5" hx-get="/get/articles" hx-trigger="load" hx-target="this" hx-swap="innerHTML">
		<h1>No articles found</h1>
		<h4 class="text-secondary">The articles that you publish will be shown here</h4>
	</div>
</div>
{{template "footer" .}}
{{end}}
